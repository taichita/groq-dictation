# 0.5系のmacOS wheelはportaudioを同梱(universal2)。Homebrew不要で動く。
sounddevice>=0.5.0
scipy>=1.11
numpy>=1.24
requests>=2.31
python-dotenv>=1.0
pyperclip>=1.8
pynput>=1.7
# 非macOS環境での自動貼り付け用（macOSではosascriptを使うため不要）
pyautogui>=0.9.54 ; sys_platform != "darwin"
# 通知領域(トレイ)の状態アイコン表示用
pystray>=0.19
pillow>=10.0
